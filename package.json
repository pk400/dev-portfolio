{
  "name": "dev-portfolio",
  "version": "1.0.0",
  "description": "Entrypoint for web app.",
  "main": "server/index.js",
  "scripts": {
    "start": "cd server && npm install && node index.js",
    "heroku-prebuild": "cd client && npm install && npm run build && cd .."
  },
  "author": "Joel Aro",
  "license": "ISC"
}
